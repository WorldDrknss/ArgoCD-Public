apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: linkerd-control-plane
  namespace: argocd
spec:
  project: default
  sources:
    - chart: linkerd-control-plane
        repoURL: https://helm.linkerd.io/edge
        targetRevision: 2024.11.8
        helm:
        valueFiles:
          - $values/argocd-linkerd/control-plane/values.yam
    - repoURL: https://github.com/WorldDrknss/ArgoCD-Public.git
      path: "argocd-linkerd/control-plane/manifests"
      ref: values
      targetRevision: main
  destination:
    server: https://kubernetes.default.svc
    namespace: linkerd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
  ignoreDifferences:
    - group: ""
      kind: Secret
      name: linkerd-identity-issuer
      namespace: linkerd
      jsonPointers:
        - /data
      type: Opaque
    - group: ""
      kind: ConfigMap
      name: linkerd-config
      namespace: linkerd
      jsonPointers:
        - /data
